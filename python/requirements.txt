# Python Analytics Service Dependencies
# Python 3.11+ required
#
# Windows: si "pip" o "python" no se reconocen, usá el launcher:  py -m pip install -r requirements.txt --pre

# Web Framework
fastapi>=0.115.0
uvicorn>=0.30.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Technical Analysis (optional; PyPI publishes pre-release builds — pin what pip can resolve)
pandas-ta>=0.4.67b0

# Visualization
streamlit>=1.37.0
plotly>=5.22.0

# Redis
redis>=5.0.0

# HTTP Client (upper bound: evita resolver a httpx 1.x pre-release y conflictos con openai/anthropic)
httpx>=0.27.0,<1

# Machine Learning (optional)
scikit-learn>=1.5.0
joblib>=1.4.0

# Market Data
yfinance>=0.2.40

# Testing
pytest>=8.3.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0